# Dedos Branding Assets

Place your custom `dedosgif.gif` file in this directory so runtime branding helpers can attach it to embeds by default.

The bot will look for the gif at `assets/branding/dedosgif.gif`. Keep the filename consistent so no additional configuration changes are required.
